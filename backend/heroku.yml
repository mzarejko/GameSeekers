build:
    docker:
        web: Dockerfile
run:
    web: python manage.py makemigrations accounts && python manage.py migrate accounts && python manage.py runserver 0.0.0.0:$PORT
release:
  image: web
  command:
    - python manage.py collectstatic 

