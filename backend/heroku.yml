build:
    docker:
        web: Dockerfile
        worker: ./prod_dockerfile
run:
    web: python3 manage.py makemigrations --no-input && python3 manage.py migrate --no-input && python3 manage.py runserver 0.0.0.0:$PORT
release:
  image: web
  command:
    - python manage.py collectstatic --noinput

